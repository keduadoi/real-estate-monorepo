{{- if .Values.postService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "post-service.fullname" . }}-hpa
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "post-service.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "post-service.fullname" . }}
  minReplicas: {{ .Values.postService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.postService.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.postService.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.postService.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.postService.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.postService.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
