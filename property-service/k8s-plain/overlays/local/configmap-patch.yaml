apiVersion: v1
kind: ConfigMap
metadata:
  name: real-estate-backend-config
  namespace: real-estate
data:
  SPRING_PROFILES_ACTIVE: "local"
  CORS_ALLOWED_ORIGINS: "http://localhost:3000,http://localhost:3001,http://localhost:30080"
