package com.realestate.backend.entity;

/**
 * Enum representing the status of a property.
 * Matches the UI PropertyStatus: 'for-sale' | 'for-rent'
 */
public enum PropertyStatus {
    FOR_SALE,
    FOR_RENT
}
