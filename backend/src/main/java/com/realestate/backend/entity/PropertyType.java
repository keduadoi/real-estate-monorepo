package com.realestate.backend.entity;

/**
 * Enum representing different types of properties.
 * Matches the UI PropertyType: 'house' | 'apartment' | 'villa' | 'townhouse'
 */
public enum PropertyType {
    HOUSE,
    APARTMENT,
    VILLA,
    TOWNHOUSE
}
