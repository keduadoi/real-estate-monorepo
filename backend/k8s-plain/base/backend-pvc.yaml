apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: real-estate-backend-backend-pvc
  namespace: real-estate
  labels:
    app: real-estate-backend
    component: backend
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
